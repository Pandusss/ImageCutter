# Telegram Emoji Pack Creator Bot

Telegram-бот на Python, который превращает загруженное пользователем изображение в набор Telegram-эмодзи (emoji pack).

## 🎯 Функциональность

1. Пользователь отправляет боту одно изображение (photo или document)
2. Бот показывает размеры и предлагает варианты сетки (адаптивно под пропорции; для квадратных — квадратные)
3. Изображение пропорционально уменьшается, чтобы поместиться в выбранную сетку, и центрируется (без растяжения)
4. Каждая часть масштабируется до 100x100px (требование Telegram)
5. Создается emoji pack через Telegram Bot API (custom_emoji) с прогресс-баром
6. Пользователь получает ссылку на готовый pack

## 📋 Требования

- Python 3.10+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Установленные зависимости (см. `requirements.txt`)

## 🚀 Установка и запуск

### 1. Клонирование/скачивание проекта

```bash
cd emojiCreator
```

### 2. Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```env
BOT_TOKEN=your_bot_token_here
```

**Как получить токен:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в файл `.env`

### 5. Запуск бота

```bash
python bot.py
```

Если всё настроено правильно, вы увидите сообщение:
```
INFO - Запуск бота...
```

## 📁 Структура проекта

```
emojiCreator/
├── bot.py                  # Основной файл бота с обработчиками
├── image_processor.py      # Модуль обработки изображений
├── emoji_pack_manager.py   # Модуль создания emoji packs
├── config.py               # Конфигурационный файл
├── requirements.txt        # Зависимости проекта
├── .env                    # Переменные окружения (создать самостоятельно)
├── .gitignore             # Игнорируемые файлы
└── README.md              # Этот файл
```

## 🔧 Настройка параметров

В файле `config.py` можно изменить следующие параметры:

```python
EMOJI_SIZE = 100              # Размер эмодзи в пикселях (требование Telegram)
MIN_FRAGMENT_SIZE = 100       # Минимальный размер фрагмента до масштабирования
```

## 📖 Использование

1. Запустите бота
2. Найдите вашего бота в Telegram
3. Отправьте команду `/start` для начала работы
4. Отправьте боту изображение (как фото или документ)
5. Дождитесь обработки
6. Получите ссылку на созданный emoji pack

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку

## 🏗️ Архитектура (кратко)

- **bot.py** — обработчики, показ адаптивных сеток, прогресс-бар создания pack.
- **image_processor.py** — подбор сетки, пропорциональное уменьшение и центрирование без растяжения, нарезка, масштабирование 100x100.
- **emoji_pack_manager.py** — создание sticker set `custom_emoji`, добавление стикеров, прогресс-callback.

## ⚠️ Обработка ошибок

Бот обрабатывает следующие ошибки:

- **Изображение слишком маленькое** - Минимальный размер 100x100px
- **Неверный формат файла** - Только изображения
- **Ошибки Telegram API** - Проблемы с созданием pack
- **Ошибки обработки изображения** - Некорректные файлы

## 🔍 Примеры использования

### Квадратное изображение
- Вход: 1024×1024
- Варианты: 3×3, 4×4, 5×5, 7×7, 10×10 (фрагменты ≥100px, до 100 эмодзи)

### Широкое изображение
- Вход: 1200×800
- Варианты (адаптивно): 4×3, 5×3, 6×4, 8×5 (до 100 эмодзи)

### Высокое изображение
- Вход: 800×1200
- Варианты (адаптивно): 3×4, 3×5, 4×6, 5×8 (до 100 эмодзи)

## 🐛 Устранение неполадок

### Бот не запускается

1. Проверьте, что токен указан в `.env`
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (должна быть 3.10+)

### Ошибка при создании emoji pack

1. Убедитесь, что бот имеет права на создание sticker sets
2. Проверьте, что у пользователя нет pack с таким же именем
3. Убедитесь, что изображение корректное и достаточно большое

### Изображение не обрабатывается

1. Проверьте формат файла (должен быть изображением)
2. Убедитесь, что размер изображения >= 100x100px
3. Попробуйте другое изображение

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Если вы нашли ошибку или хотите предложить улучшение, создайте issue или pull request.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Проверьте логи бота для диагностики ошибок

---

**Приятного использования! 🎨**
